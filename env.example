# NuGet Publishing Configuration
# Copy this file to .env and fill in your actual values

# Your NuGet API key (get this from https://www.nuget.org/account/apikeys)
NUGET_API_KEY=your_nuget_api_key_here

# NuGet source URL (defaults to NuGet.org if not specified)
# For NuGet.org: https://api.nuget.org/v3/index.json
# For private feeds: your_private_feed_url
NUGET_SOURCE=https://api.nuget.org/v3/index.json

# Optional: Package version override (if not specified, uses version from .csproj)
# PACKAGE_VERSION=1.0.1

# Optional: Build configuration (defaults to Release)
# BUILD_CONFIGURATION=Release

